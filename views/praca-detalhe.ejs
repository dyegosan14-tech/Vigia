<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <%- include('partials/head', { titulo }) %>
</head>
<body>
  <% const statusClasse = (s) => (s === 'operacional' ? 'ok' : (s || 'ok')); %>
  <% const statusTexto = { operacional: 'Operacional', atencao: 'Atenção', critico: 'Crítico' }; %>
  <% const detalheEquipamentos = (praca.equipamentos && Array.isArray(praca.equipamentos.detalhe)) ? praca.equipamentos.detalhe : []; %>
  <% const totalEquip = (praca.equipamentos && Number(praca.equipamentos.total)) || 0; %>
  <% const operacionaisEquip = (praca.equipamentos && Number(praca.equipamentos.operacionais)) || 0; %>
  <% const custoManutencao = detalheEquipamentos.reduce((s, eq) => s + (Number(eq.quantidade) || 0) * (Number(eq.custo_manutencao_unitario) || 0), 0); %>

  <div class="app">
    <%- include('partials/sidebar') %>

    <main class="main">
      <header class="topbar">
        <div class="topbar-left">
          <button class="mobile-nav-toggle" id="btn-toggle-menu" aria-label="Abrir menu" aria-expanded="false" aria-controls="app-sidebar">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="3" y1="12" x2="21" y2="12"></line>
              <line x1="3" y1="6" x2="21" y2="6"></line>
              <line x1="3" y1="18" x2="21" y2="18"></line>
            </svg>
          </button>
          <div>
            <div class="breadcrumb">
              <a href="/pracas">Praças do Recife (EMLURB)</a>
              <span>/</span>
              <span><%= praca.nome %></span>
            </div>
            <h1 class="topbar-title" style="display: flex; align-items: center; gap: 10px;">
              <%= praca.nome %>
              <span class="status-label <%= statusClasse(praca.status) %>" style="font-size: 11px;">
                <%= statusTexto[praca.status] || praca.status %>
              </span>
            </h1>
          </div>
        </div>

        <div class="topbar-right">
          <div class="telemetry-time">
            <div class="clock" id="relogio"></div>
            <div class="update-status">Bairro: <%= praca.bairro %> &middot; Recife/PE</div>
          </div>
        </div>
      </header>

      <div class="content">
        <!-- Indicadores da Praça -->
        <div class="kpi-grid" style="grid-template-columns: repeat(auto-fit, minmax(190px, 1fr));">
          <!-- Ocupação -->
          <div class="kpi-card pcr-accent">
            <div class="kpi-card-header">
              <span class="label">Taxa de Frequência Local</span>
              <div class="kpi-icon-wrap">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                  <circle cx="9" cy="7" r="4"></circle>
                </svg>
              </div>
            </div>
            <div class="kpi-value-row">
              <span class="value"><%= Number(praca.ocupacao_atual) || 0 %></span>
              <span class="unit">%</span>
            </div>
            <div class="kpi-footer">
              <span class="trend-badge neutral">Fluxo comunitário</span>
            </div>
          </div>

          <!-- Saúde dos Dispositivos -->
          <div class="kpi-card <%= (totalEquip > 0 && operacionaisEquip === totalEquip) ? 'ok' : 'warn' %>">
            <div class="kpi-card-header">
              <span class="label">Dispositivos em Operação</span>
              <div class="kpi-icon-wrap">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="2" y="2" width="20" height="8" rx="2" ry="2"></rect>
                  <rect x="2" y="14" width="20" height="8" rx="2" ry="2"></rect>
                  <line x1="6" y1="6" x2="6.01" y2="6"></line>
                  <line x1="6" y1="18" x2="6.01" y2="18"></line>
                </svg>
              </div>
            </div>
            <div class="kpi-value-row">
              <span class="value"><%= operacionaisEquip %></span>
              <span class="unit">/ <%= totalEquip %> ativos</span>
            </div>
            <div class="kpi-footer">
              <span class="trend-badge <%= (totalEquip > 0 && operacionaisEquip === totalEquip) ? 'positive' : 'negative' %>">
                <%= totalEquip > 0 ? Math.round((operacionaisEquip / totalEquip) * 100) : 0 %>% em serviço
              </span>
            </div>
          </div>

          <!-- Percepção Cidadã -->
          <div class="kpi-card">
            <div class="kpi-card-header">
              <span class="label">Avaliação dos Frequentadores</span>
              <div class="kpi-icon-wrap">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
                </svg>
              </div>
            </div>
            <div class="kpi-value-row">
              <span class="value"><%= praca.percepcao_seguranca || '—' %></span>
              <span class="unit">/ 10</span>
            </div>
            <div class="kpi-footer">
              <span class="trend-badge positive">Índice Conecta Recife</span>
            </div>
          </div>

          <!-- Conservação EMLURB -->
          <div class="kpi-card pcr-accent">
            <div class="kpi-card-header">
              <span class="label">Custo Estimado EMLURB</span>
              <div class="kpi-icon-wrap">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <line x1="12" y1="1" x2="12" y2="23"></line>
                  <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path>
                </svg>
              </div>
            </div>
            <div class="kpi-value-row">
              <span class="value" style="font-size: 21px;">R$ <%= custoManutencao.toLocaleString('pt-BR') %></span>
              <span class="unit">/mês</span>
            </div>
            <div class="kpi-footer">
              <span class="trend-badge neutral">Contrato de conservação</span>
            </div>
          </div>
        </div>

        <!-- Monitor de Câmeras CFTV (Padrão VMS Profissional do COP) -->
        <div class="panel" style="margin-bottom: 24px;">
          <div class="panel-header">
            <div class="panel-header-left">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--pcr-yellow)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M23 7l-7 5 7 5V7z"></path>
                <rect x="1" y="5" width="15" height="14" rx="2" ry="2"></rect>
              </svg>
              <h2>Central de Vídeo COP Recife &middot; <%= praca.nome %></h2>
            </div>
            <span style="font-size: 11.5px; font-weight: 600; color: var(--status-ok);">
              ● 2 Canais Conectados
            </span>
          </div>

          <div class="cctv-mosaic">
            <!-- Canal 01 -->
            <div class="cctv-tile">
              <div class="cctv-overlay-top">
                <div class="cctv-cam-title">
                  <span class="cctv-online-dot"></span>
                  CANAL 01 &middot; ALAMEDA PRINCIPAL
                </div>
                <div class="cctv-codec-badge">1080p &middot; 30 FPS &middot; H.264</div>
              </div>

              <!-- Indicador discreto de stream -->
              <div style="text-align: center; color: var(--text-muted); font-size: 12px; z-index: 1;">
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" style="margin: 0 auto 6px; display: block; opacity: 0.6;">
                  <polygon points="5 3 19 12 5 21 5 3"></polygon>
                </svg>
                Transmissão em Tempo Real &middot; Servidor COP
              </div>

              <div class="cctv-overlay-bottom">
                <div class="cctv-timestamp cctv-clock-sync">--/--/---- --:--:--</div>
                <div class="cctv-controls-mock">
                  <span class="cctv-btn-pill">PRESET 01</span>
                  <span class="cctv-btn-pill">PTZ</span>
                </div>
              </div>
            </div>

            <!-- Canal 02 -->
            <div class="cctv-tile">
              <div class="cctv-overlay-top">
                <div class="cctv-cam-title">
                  <span class="cctv-online-dot"></span>
                  CANAL 02 &middot; ÁREA DE CONVIVÊNCIA
                </div>
                <div class="cctv-codec-badge">1080p &middot; 30 FPS &middot; H.264</div>
              </div>

              <div style="text-align: center; color: var(--text-muted); font-size: 12px; z-index: 1;">
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" style="margin: 0 auto 6px; display: block; opacity: 0.6;">
                  <polygon points="5 3 19 12 5 21 5 3"></polygon>
                </svg>
                Transmissão em Tempo Real &middot; Servidor COP
              </div>

              <div class="cctv-overlay-bottom">
                <div class="cctv-timestamp cctv-clock-sync">--/--/---- --:--:--</div>
                <div class="cctv-controls-mock">
                  <span class="cctv-btn-pill">PRESET 02</span>
                  <span class="cctv-btn-pill">PTZ</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="grid-2">
          <!-- Equipamentos Instalados (EMLURB / GMR) -->
          <div class="panel">
            <div class="panel-header">
              <div class="panel-header-left">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--pcr-blue-light)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"></path>
                </svg>
                <h2>Inventário de Dispositivos (EMLURB / GMR)</h2>
              </div>
              <span class="meta"><%= detalheEquipamentos.length %> categorias cadastradas</span>
            </div>

            <div class="equip-grid">
              <% if (detalheEquipamentos.length === 0) { %>
                <div class="empty-state" style="grid-column: 1 / -1;">Nenhum detalhe de equipamento cadastrado.</div>
              <% } %>
              <% detalheEquipamentos.forEach((eq) => {
                   const qtd = Number(eq.quantidade) || 0;
                   const op = Number(eq.operacionais) || 0;
                   const pct = qtd > 0 ? (op / qtd) * 100 : 0;
                   const estadoSaude = pct === 100 ? 'ok' : pct >= 50 ? 'warn' : 'critical';
                   const custoTotal = qtd * (Number(eq.custo_manutencao_unitario) || 0);
                   const nomeTipo = String(eq.tipo || '');
              %>
                <div class="equip-card">
                  <div class="equip-card-top">
                    <span class="equip-card-name">
                      <% if (nomeTipo.includes('Camera')) { %>📹<% } %>
                      <% if (nomeTipo.includes('Iluminacao')) { %>💡<% } %>
                      <% if (nomeTipo.includes('panico')) { %>🚨<% } %>
                      <% if (nomeTipo.includes('fluxo')) { %>📡<% } %>
                      <%= nomeTipo %>
                    </span>
                    <span class="status-label <%= estadoSaude %>">
                      <%= op %> / <%= qtd %>
                    </span>
                  </div>
                  <div class="equip-health-bar">
                    <div class="equip-health-fill <%= estadoSaude %>" style="width: <%= pct %>%;"></div>
                  </div>
                  <div class="equip-card-footer">
                    <span>Taxa: <%= pct.toFixed(0) %>% ativo</span>
                    <span>R$ <%= custoTotal.toLocaleString('pt-BR') %>/mês</span>
                  </div>
                </div>
              <% }) %>
            </div>

            <!-- Despacho Municipal Rápido -->
            <div style="padding: 16px 20px; border-top: 1px solid var(--border-subtle); display: flex; gap: 10px; flex-wrap: wrap;">
              <button class="btn primary" onclick="alert('Solicitação de ronda preventiva enviada ao Centro de Despacho da Guarda Municipal do Recife (GMR).')">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                </svg>
                Despachar Viatura GMR
              </button>
              <button class="btn" onclick="alert('Ordem de serviço de reparo aberta no sistema 156 da EMLURB.')">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"></path>
                </svg>
                Abrir Chamado EMLURB
              </button>
            </div>
          </div>

          <!-- Histórico de Chamados e Ocorrências -->
          <div class="panel">
            <div class="panel-header">
              <div class="panel-header-left">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--pcr-yellow)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="10"></circle>
                  <line x1="12" y1="8" x2="12" y2="12"></line>
                  <line x1="12" y1="16" x2="12.01" y2="16"></line>
                </svg>
                <h2>Histórico de Chamados no Local</h2>
              </div>
              <span class="meta"><%= Array.isArray(eventos) ? eventos.length : 0 %> registros</span>
            </div>
            <div class="panel-body" style="max-height: 400px; overflow-y: auto;">
              <% if (!Array.isArray(eventos) || eventos.length === 0) { %>
                <div class="empty-state">Nenhum evento registrado nesta praça até o momento.</div>
              <% } else { %>
                <% eventos.forEach((o) => {
                     const hora = o && o.timestamp ? new Date(o.timestamp).toLocaleString('pt-BR', { dateStyle: 'short', timeStyle: 'short' }) : '—';
                %>
                  <div class="event-row">
                    <div class="event-top">
                      <span class="event-time"><%= hora %></span>
                      <span class="badge <%= o.severidade || 'media' %>"><%= o.severidade || 'media' %></span>
                      <span class="badge status-<%= o.status || 'aberto' %>"><%= String(o.status || 'aberto').replace('_', ' ') %></span>
                      <% if (o.origem === 'cidadao') { %>
                        <span class="badge origem-cidadao">Conecta Recife</span>
                      <% } %>
                    </div>
                    <div class="event-tipo"><%= o.tipo %></div>
                    <% if (o.descricao) { %>
                      <div style="font-size: 12px; color: var(--text-muted); margin-top: 2px;">
                        "<%= o.descricao %>"
                      </div>
                    <% } %>
                  </div>
                <% }) %>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>

  <script src="/js/dashboard.js"></script>
  <script>
    function atualizarCCTVTicks() {
      const elClocks = document.querySelectorAll('.cctv-clock-sync');
      const agora = new Date();
      const str = agora.toLocaleDateString('pt-BR') + ' ' + agora.toLocaleTimeString('pt-BR');
      elClocks.forEach(c => c.textContent = str);
    }
    setInterval(atualizarCCTVTicks, 1000);
    atualizarCCTVTicks();
  </script>
</body>
</html>
