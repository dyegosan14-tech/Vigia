PORT=3000
SESSION_SECRET=troque-este-valor-em-producao
DEMO_USER=operador
DEMO_PASS=vigia2026

# Token que os sensores/cameras (camada de borda) devem enviar no header
# "Authorization: Bearer <token>" ao chamar POST /api/eventos.
# Em producao, use um token por dispositivo em vez de um valor unico.
DEVICE_TOKEN=vigia-sensor-dev-token
